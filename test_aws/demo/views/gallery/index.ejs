<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Wildlife Gallery</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    :root {
      --primary-color: #6C3CE1;
      --secondary-color: #00B4D8;
      --accent-color: #FF6B6B;
      --accent-orange: #FF9F43;
      --accent-pink: #F368E0;
      --accent-teal: #0ABDE3;
      --accent-green: #10AC84;
      --accent-yellow: #FECA57;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 30%, #a1c4fd 60%, #c2e9fb 100%);
      background-attachment: fixed;
      min-height: 100vh;
    }

    .navbar {
      background: linear-gradient(135deg, #6C3CE1, #00B4D8, #FF6B6B) !important;
      box-shadow: 0 4px 20px rgba(108, 60, 225, 0.3);
    }

    .creature-card {
      border: none;
      border-radius: 20px;
      overflow: hidden;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      height: 100%;
      background: white;
      border-bottom: 4px solid transparent;
    }

    .creature-card:hover {
      transform: translateY(-10px) rotate(-1deg);
      box-shadow: 0 20px 40px rgba(108, 60, 225, 0.2);
      border-bottom: 4px solid var(--accent-color);
    }

    .creature-item:nth-child(4n+1) .creature-card:hover { border-bottom-color: #FF6B6B; }
    .creature-item:nth-child(4n+2) .creature-card:hover { border-bottom-color: #FECA57; }
    .creature-item:nth-child(4n+3) .creature-card:hover { border-bottom-color: #0ABDE3; }
    .creature-item:nth-child(4n+4) .creature-card:hover { border-bottom-color: #10AC84; }

    .creature-card img {
      height: 200px;
      object-fit: cover;
      transition: transform 0.5s ease;
    }

    .creature-card:hover img {
      transform: scale(1.1);
    }

    .card-img-overlay-custom {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      background: linear-gradient(transparent, rgba(0,0,0,0.8));
      padding: 20px;
      color: white;
    }

    .category-badge {
      display: inline-block;
      padding: 5px 15px;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: bold;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .category-animal {
      background: linear-gradient(135deg, #FF6B6B, #EE5A24);
      color: white;
    }

    .category-bird {
      background: linear-gradient(135deg, #0ABDE3, #6C3CE1);
      color: white;
    }

    .filter-btn {
      border-radius: 25px;
      padding: 8px 20px;
      margin: 5px;
      transition: all 0.3s ease;
      border: 2px solid #ddd;
      font-weight: 600;
    }

    .filter-btn:hover {
      border-color: var(--accent-pink);
      color: var(--accent-pink);
      transform: translateY(-2px);
    }

    .filter-btn.active {
      background: linear-gradient(135deg, #6C3CE1, #F368E0);
      color: white;
      border-color: transparent;
      box-shadow: 0 4px 15px rgba(108, 60, 225, 0.3);
    }

    .search-box {
      border-radius: 25px;
      padding: 10px 25px;
      border: 2px solid rgba(255,255,255,0.6);
      background: rgba(255,255,255,0.9);
      transition: all 0.3s ease;
    }

    .search-box:focus {
      border-color: var(--accent-yellow);
      box-shadow: 0 0 0 4px rgba(254, 202, 87, 0.3);
      background: white;
    }

    .hero-section {
      background: linear-gradient(135deg, #6C3CE1 0%, #00B4D8 30%, #10AC84 60%, #FF9F43 100%);
      color: white;
      padding: 70px 0;
      margin-bottom: 40px;
      position: relative;
      overflow: hidden;
    }

    .hero-section::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 50%);
      animation: shimmer 8s ease-in-out infinite;
    }

    @keyframes shimmer {
      0%, 100% { transform: translate(0, 0); }
      50% { transform: translate(25%, 25%); }
    }

    .hero-section h1 {
      text-shadow: 0 2px 10px rgba(0,0,0,0.2);
    }

    .hero-section .lead {
      opacity: 0.9;
    }

    .stats-bar {
      display: flex;
      justify-content: center;
      gap: 40px;
      margin-top: 25px;
    }

    .stat-item {
      text-align: center;
    }

    .stat-number {
      font-size: 2rem;
      font-weight: 800;
      color: var(--accent-yellow);
    }

    .stat-label {
      font-size: 0.85rem;
      opacity: 0.85;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .card-title {
      color: #2d3436;
    }

    .creature-item:nth-child(4n+1) .card-title { color: #6C3CE1; }
    .creature-item:nth-child(4n+2) .card-title { color: #EE5A24; }
    .creature-item:nth-child(4n+3) .card-title { color: #0ABDE3; }
    .creature-item:nth-child(4n+4) .card-title { color: #10AC84; }

    /* Chat Widget Styles */
    .chat-widget {
      position: fixed;
      bottom: 20px;
      right: 20px;
      z-index: 1000;
    }

    .chat-toggle {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      background: linear-gradient(135deg, #FF6B6B, #F368E0);
      border: none;
      color: white;
      font-size: 24px;
      cursor: pointer;
      box-shadow: 0 5px 20px rgba(255, 107, 107, 0.4);
      transition: transform 0.3s ease;
    }

    .chat-toggle:hover {
      transform: scale(1.1) rotate(10deg);
    }

    .chat-window {
      display: none;
      position: fixed;
      bottom: 90px;
      right: 20px;
      width: 380px;
      height: 500px;
      background: white;
      border-radius: 20px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.2);
      flex-direction: column;
      overflow: hidden;
    }

    .chat-window.open {
      display: flex;
    }

    .chat-header {
      background: linear-gradient(135deg, #6C3CE1, #FF6B6B);
      color: white;
      padding: 15px 20px;
      font-weight: bold;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .chat-messages {
      flex: 1;
      overflow-y: auto;
      padding: 15px;
      background: #f8f9fa;
    }

    .chat-message {
      margin-bottom: 15px;
      display: flex;
    }

    .chat-message.user {
      justify-content: flex-end;
    }

    .chat-message .bubble {
      max-width: 80%;
      padding: 12px 16px;
      border-radius: 18px;
      font-size: 14px;
      line-height: 1.4;
    }

    .chat-message.user .bubble {
      background: linear-gradient(135deg, #6C3CE1, #0ABDE3);
      color: white;
      border-bottom-right-radius: 4px;
    }

    .chat-message.bot .bubble {
      background: white;
      color: #333;
      border-bottom-left-radius: 4px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
    }

    .chat-input-area {
      padding: 15px;
      background: white;
      border-top: 1px solid #eee;
      display: flex;
      gap: 10px;
    }

    .chat-input-area input {
      flex: 1;
      border: 1px solid #ddd;
      border-radius: 25px;
      padding: 10px 20px;
      outline: none;
    }

    .chat-input-area input:focus {
      border-color: var(--secondary-color);
    }

    .chat-input-area button {
      width: 45px;
      height: 45px;
      border-radius: 50%;
      background: linear-gradient(135deg, #FF6B6B, #F368E0);
      border: none;
      color: white;
      cursor: pointer;
      transition: transform 0.2s ease;
    }

    .chat-input-area button:hover {
      transform: scale(1.1);
    }

    .typing-indicator {
      display: flex;
      gap: 4px;
      padding: 12px 16px;
    }

    .typing-indicator span {
      width: 8px;
      height: 8px;
      background: #ccc;
      border-radius: 50%;
      animation: bounce 1.4s infinite ease-in-out both;
    }

    .typing-indicator span:nth-child(1) { animation-delay: -0.32s; }
    .typing-indicator span:nth-child(2) { animation-delay: -0.16s; }

    @keyframes bounce {
      0%, 80%, 100% { transform: scale(0); }
      40% { transform: scale(1); }
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/gallery">
        <i class="bi bi-feather me-2"></i>Wildlife Gallery
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <% if (user && user.isAdmin) { %>
          <li class="nav-item">
            <a class="nav-link" href="/admin">
              <i class="bi bi-gear me-1"></i>Admin
            </a>
          </li>
          <% } %>
          <li class="nav-item">
            <span class="nav-link">
              <i class="bi bi-person-circle me-1"></i>Welcome, <%= user.username %>
              <% if (user && user.isAdmin) { %><span class="badge bg-warning text-dark ms-1">Admin</span><% } %>
            </span>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/auth/logout">
              <i class="bi bi-box-arrow-right me-1"></i>Logout
            </a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <div class="hero-section text-center">
    <div class="container position-relative">
      <h1 class="display-4 fw-bold mb-3">
        <i class="bi bi-globe-americas me-3"></i>Explore Wildlife
      </h1>
      <p class="lead mb-4">Discover amazing animals and birds from around the world</p>

      <!-- Search Box -->
      <div class="row justify-content-center mb-3">
        <div class="col-md-6">
          <div class="input-group">
            <input type="text" class="form-control search-box" id="searchInput" placeholder="Search animals and birds...">
            <button class="btn btn-light" type="button" onclick="searchCreatures()" style="border-radius: 0 25px 25px 0;">
              <i class="bi bi-search"></i>
            </button>
          </div>
        </div>
      </div>

      <!-- Stats Bar -->
      <div class="stats-bar">
        <div class="stat-item">
          <div class="stat-number"><%= creatures.length %></div>
          <div class="stat-label">Species</div>
        </div>
        <div class="stat-item">
          <div class="stat-number"><%= categories.length %></div>
          <div class="stat-label">Categories</div>
        </div>
        <div class="stat-item">
          <div class="stat-number">&infin;</div>
          <div class="stat-label">Wonder</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Main Content -->
  <div class="container">
    <% if (error && error.length > 0) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>
    <% if (success && success.length > 0) { %>
      <div class="alert alert-success"><%= success %></div>
    <% } %>

    <!-- Category Filters -->
    <div class="text-center mb-4">
      <a href="/gallery" class="btn btn-outline-secondary filter-btn <%= typeof currentCategory === 'undefined' ? 'active' : '' %>">
        <i class="bi bi-grid me-1"></i>All
      </a>
      <% categories.forEach(function(cat) { %>
        <a href="/gallery/category/<%= cat.id %>" class="btn btn-outline-secondary filter-btn <%= (typeof currentCategory !== 'undefined' && currentCategory && currentCategory.id === cat.id) ? 'active' : '' %>">
          <i class="bi bi-<%= cat.name.toLowerCase() === 'birds' ? 'feather' : 'heart' %> me-1"></i><%= cat.name %>
        </a>
      <% }); %>
    </div>

    <!-- Creatures Grid -->
    <div class="row g-4" id="creaturesGrid">
      <% if (creatures.length === 0) { %>
        <div class="col-12 text-center py-5">
          <i class="bi bi-emoji-frown display-1 text-muted"></i>
          <h3 class="text-muted mt-3">No creatures found</h3>
          <p class="text-muted">Try a different search or category</p>
        </div>
      <% } else { %>
        <% creatures.forEach(function(creature) { %>
          <div class="col-md-6 col-lg-4 col-xl-3 creature-item">
            <a href="/gallery/creature/<%= creature.id %>" class="text-decoration-none">
              <div class="card creature-card">
                <div class="position-relative overflow-hidden">
                  <img src="<%= creature.image_url || 'https://placehold.co/400x300/2d5016/white?text=' + encodeURIComponent(creature.name) %>"
                       class="card-img-top"
                       alt="<%= creature.name %>"
                       onerror="this.src='https://placehold.co/400x300/2d5016/white?text=<%= encodeURIComponent(creature.name) %>'">
                  <div class="card-img-overlay-custom">
                    <span class="category-badge category-<%= creature.category_name.toLowerCase() === 'birds' ? 'bird' : 'animal' %>">
                      <%= creature.category_name %>
                    </span>
                  </div>
                </div>
                <div class="card-body">
                  <h5 class="card-title fw-bold"><%= creature.name %></h5>
                  <p class="card-text text-muted small mb-2">
                    <em><%= creature.scientific_name || 'Scientific name unknown' %></em>
                  </p>
                  <p class="card-text text-muted">
                    <%= creature.description ? creature.description.substring(0, 80) + '...' : 'Click to learn more' %>
                  </p>
                  <div class="d-flex justify-content-between align-items-center">
                    <small class="text-muted">
                      <i class="bi bi-geo-alt me-1"></i><%= creature.habitat ? creature.habitat.split(',')[0] : 'Unknown' %>
                    </small>
                    <span class="badge bg-light text-dark">
                      <i class="bi bi-arrow-right"></i>
                    </span>
                  </div>
                </div>
              </div>
            </a>
          </div>
        <% }); %>
      <% } %>
    </div>
  </div>

  <!-- Chat Widget -->
  <div class="chat-widget">
    <div class="chat-window" id="chatWindow">
      <div class="chat-header">
        <span><i class="bi bi-robot me-2"></i>Wildlife Assistant</span>
        <button onclick="toggleChat()" style="background: none; border: none; color: white; font-size: 20px;">
          <i class="bi bi-x-lg"></i>
        </button>
      </div>
      <div class="chat-messages" id="chatMessages">
        <div class="chat-message bot">
          <div class="bubble">
            Hello! I'm your wildlife assistant. Ask me anything about animals and birds! 🦁🦅
          </div>
        </div>
      </div>
      <div class="chat-input-area">
        <input type="text" id="chatInput" placeholder="Ask about animals or birds..." onkeypress="handleKeyPress(event)">
        <button onclick="sendMessage()">
          <i class="bi bi-send"></i>
        </button>
      </div>
    </div>
    <button class="chat-toggle" onclick="toggleChat()" id="chatToggle">
      <i class="bi bi-chat-dots"></i>
    </button>
  </div>

  <!-- Footer -->
  <footer style="background: linear-gradient(135deg, #2d3436, #6C3CE1); color: #ddd; padding: 25px 0; margin-top: 50px;">
    <div class="container text-center">
      <p class="mb-0">
        <i class="bi bi-feather me-2" style="color: #FF6B6B;"></i>
        <span style="font-weight: 600;">Wildlife Gallery</span> &copy; 2024
        <span style="margin-left: 10px; color: #FECA57;">&#10024;</span>
      </p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Search functionality
    function searchCreatures() {
      const query = document.getElementById('searchInput').value.toLowerCase();
      const items = document.querySelectorAll('.creature-item');

      items.forEach(item => {
        const name = item.querySelector('.card-title').textContent.toLowerCase();
        const description = item.querySelector('.card-text').textContent.toLowerCase();
        if (name.includes(query) || description.includes(query)) {
          item.style.display = 'block';
        } else {
          item.style.display = 'none';
        }
      });
    }

    document.getElementById('searchInput').addEventListener('keyup', function(e) {
      if (e.key === 'Enter') {
        searchCreatures();
      }
      // Live search
      searchCreatures();
    });

    // Chat Widget JavaScript
    let chatOpen = false;

    function toggleChat() {
      chatOpen = !chatOpen;
      document.getElementById('chatWindow').classList.toggle('open', chatOpen);
      document.getElementById('chatToggle').innerHTML = chatOpen
        ? '<i class="bi bi-x-lg"></i>'
        : '<i class="bi bi-chat-dots"></i>';
    }

    function handleKeyPress(e) {
      if (e.key === 'Enter') {
        sendMessage();
      }
    }

    async function sendMessage() {
      const input = document.getElementById('chatInput');
      const message = input.value.trim();
      if (!message) return;

      addMessage(message, 'user');
      input.value = '';
      showTyping();

      try {
        const response = await fetch('/chat', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ message, context: 'Gallery page - browsing all animals and birds' })
        });

        const data = await response.json();
        hideTyping();

        if (data.error) {
          addMessage(data.error, 'bot');
        } else {
          addMessage(data.reply, 'bot');
        }
      } catch (error) {
        hideTyping();
        addMessage('Sorry, there was an error. Please try again.', 'bot');
      }
    }

    function addMessage(text, type) {
      const messagesContainer = document.getElementById('chatMessages');
      const messageDiv = document.createElement('div');
      messageDiv.className = `chat-message ${type}`;
      messageDiv.innerHTML = `<div class="bubble">${escapeHtml(text)}</div>`;
      messagesContainer.appendChild(messageDiv);
      messagesContainer.scrollTop = messagesContainer.scrollHeight;
    }

    function showTyping() {
      const messagesContainer = document.getElementById('chatMessages');
      const typingDiv = document.createElement('div');
      typingDiv.className = 'chat-message bot';
      typingDiv.id = 'typingIndicator';
      typingDiv.innerHTML = '<div class="bubble typing-indicator"><span></span><span></span><span></span></div>';
      messagesContainer.appendChild(typingDiv);
      messagesContainer.scrollTop = messagesContainer.scrollHeight;
    }

    function hideTyping() {
      const typing = document.getElementById('typingIndicator');
      if (typing) typing.remove();
    }

    function escapeHtml(text) {
      const div = document.createElement('div');
      div.textContent = text;
      return div.innerHTML;
    }
  </script>
</body>
</html>
